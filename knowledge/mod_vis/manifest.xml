<package>
  <description brief="mod_vis">

     mod_vis

     Visualisation module for the Prolog knowledge base.

     Instances in the knowledge base can be pushed to the visualization
     canvas, are then drawn and can be further inspected.

     Currently supported: Action sequences, linked to the resp. human pose
     sequence, as well as most objects that are relevant in the IAS kitchen.

  </description>
  <author>Moritz Tenorth, Jakob Engel</author>
  <license>GPL</license>
  <review status="unreviewed" notes=""/>
  <url>http://ias.in.tum.de/kb/wiki/index.php/mod_vis</url>

  <depend package="inf9_knowledge_base"/>

  <export>
    <java classpath="${prefix}/lib/vis.jar:${prefix}/lib/controlP5.jar:${prefix}/lib/core.jar:${prefix}/lib/srldb.jar:${prefix}/lib/vecmath.jar:${prefix}/lib/tumutils.jar"/>
  </export>

</package>
